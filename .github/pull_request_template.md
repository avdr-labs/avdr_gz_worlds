## Summary

<!-- What does this PR do and why? 1-3 sentences. -->

Jira: [AVDRPOC-XX](https://your-org.atlassian.net/browse/AVDRPOC-XX)

## Changes

<!-- Bullet the notable changes. Call out new/renamed packages, nodes, topics, params, or interfaces. -->
-

## Type of change

- [ ] Bug fix (non-breaking)
- [ ] New feature (non-breaking)
- [ ] Breaking change (changes existing topics/services/actions, params, or TF)
- [ ] Infrastructure / tooling / CI
- [ ] Docs only

## ROS interface impact

<!-- Fill in anything that changed. Write "none" if not applicable. -->
- **Packages touched:**
- **Nodes added/changed:**
- **Topics / services / actions:**
- **Parameters (new/renamed/removed, with defaults):**
- **Messages / interfaces (`.msg`/`.srv`/`.action`):**
- **TF frames / URDF / xacro:**
- **Launch files / config (yaml):**

## How it was tested

- [ ] `colcon build` succeeds
- [ ] `colcon test` / linters pass (`ament_lint`, `flake8`, `clang-format`)
- [ ] Ran in simulation (Gazebo)
- [ ] Ran on hardware
- [ ] Verified via `ros2 launch ...` / `ros2 topic echo` / rviz

**Test steps / commands:**
